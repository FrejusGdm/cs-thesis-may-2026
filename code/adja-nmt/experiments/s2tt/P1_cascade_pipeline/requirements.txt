# P1 Cascade Pipeline — Python dependencies
# Install: pip install -r requirements.txt
# TTS (Spark) also needs: pip install unsloth
# Gemini modes need: pip install google-generativeai

pyyaml>=6.0
soundfile>=0.12.1
numpy>=1.24
transformers>=4.40.0
torch>=2.1.0
huggingface-hub>=0.20.0
sacrebleu>=2.3.0
# google-generativeai>=0.5.0  # uncomment when using Gemini response/MT modes
