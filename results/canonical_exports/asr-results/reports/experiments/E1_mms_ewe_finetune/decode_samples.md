# E1: Decode Samples

No samples — the job never reached the first evaluation step.

Run ended inside the first training forward pass with `CUDA out of memory`. No hypotheses were decoded. See `training_curve.md` and `conclusion.md` for what went wrong.
