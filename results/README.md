# Results

This directory contains thesis-facing summaries plus a plan for consistent
public result artifact organization.

Current result repos are mixed: some are model repos, some are dataset repos,
and some are nested experiment dumps. The release should preserve those as
private provenance and export clean canonical result repos later.
